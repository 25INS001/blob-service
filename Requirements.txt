flask==3.0.3
boto3==1.42.15
python-dotenv==1.0.1
requests
Flask-SQLAlchemy
psycopg2-binary
flask-socketio
eventlet
# psycopg2 is a C extension, so eventlet.monkey_patch() cannot reach it.
# Without this the sockets cooperate and every database query still blocks the
# event loop, which is the half-fix that looks like it worked.
psycogreen
paramiko
gevent
gevent-websocket
